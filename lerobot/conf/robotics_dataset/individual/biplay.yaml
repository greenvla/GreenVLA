
_target_: lerobot.common.datasets.data_config.LeRobotAlohaDataConfig

# Dataset identification
name: biplay
asset_id: biplay
repo_id: biplay
root_dir: <path_to_biplay_dataset>
episodes_list_file: ${.root_dir}/filter/v1/filtered_episodes.json
validation_episodes: ${.root_dir}/meta/validation_episodes.json

# Action configuration
action_sequence_keys: [action]
action_horizon: 10
action_offset: 0
action_sample_step: 2
state_dim: 14
adapt_to_pi: true
control_mode: joint
map_to_unified_space: false
# Base configuration
base_config:
  _target_: lerobot.common.datasets.data_config.DataConfig
  local_files_only: true
  prompt_from_task: true 